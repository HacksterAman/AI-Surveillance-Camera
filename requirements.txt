opencv-python>=4.8.0
numpy>=1.21.0
insightface>=0.7.3
PyQt5>=5.15.0

# GPU Acceleration (use onnxruntime-gpu for NVIDIA GPUs)
onnxruntime-gpu==1.20.0

# PostgreSQL with pgvector for face embeddings storage
psycopg2-binary>=2.9.9
pgvector>=0.2.4

# PyTorch for GPU acceleration (CUDA 12.6)
# Install separately:
# pip3 install torch torchvision --index-url https://download.pytorch.org/whl/cu126
